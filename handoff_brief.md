# 関係人口ダッシュボード｜ブラッシュアップ用ハンドオフ資料

> このドキュメントの目的：本ダッシュボードのデザイン改善を**別のAIに引き継ぐ**ための、背景・確定事項・現状仕様・改善方針・データ構造の一式。受け取ったAIは、まず「4. 確定済みの設計判断」と「8. やってはいけないこと」を必ず読んでから着手すること。

---

## 1. プロジェクト背景

- **何を作っているか**：和歌山県のあるまち（白浜を想定。ただし**地名は画面に一切出さない**）で、**閑散期（冬）に行う地域周遊イベント**の参加データを可視化するダッシュボード。
- **イベントの仕組み**：観光スポットや飲食店にポスターを設置し、ポスターに埋め込まれた**NFCタグ**にスマホをかざすとアプリ内にログが記録される。指定スポットを巡ると景品がもらえる。
- **基盤サービス**：「ろかまっぷ。」（合同会社Local Bridge）。観光パンフレットを地図上にマッピングしたサービスで、共通アカウント「**ろかID**」で会員を管理する。本ダッシュボードは**ろかまっぷ。への機能拡張**として位置づけ、**自治体（観光課）へ提案**する想定。
- **差別化の核（重要・デザイン優先順位の前提）**：デジタルスタンプラリーの集計ツールは既に多数あり、「集計ダッシュボード」自体は差別化にならない。武器は**ろかIDが単発イベントで消えない継続アカウントである**こと。よって画面の主役は個々のグラフではなく、**「関係人口化のファネル（深まり）」**である。ここを弱めないこと。

---

## 2. ゴールとKPI

- **ゴールKPI：関係人口の拡大**。
- **必須の前提**：関係人口は1イベントで完成しない（総務省定義上「地域に継続的に関わる人」）。さらに**今回は初開催のため過去比較ができない**。したがってダッシュボードは「関係人口そのものの数」ではなく、**1回のイベント内での"関わりの深まり"の段階**を見る建て付けにしている。

---

## 3. 取得できるデータ（制約）

ろかIDから取得可能：
- 訪れたスポット
- 訪れた日時
- アカウント名（個人名）
- 性別
- 年代（◯◯代）
- 生年月日
- 出身地

将来追加したい（未実装）：**国籍**（インバウンド分析用）。

データの性質：
- **ID登録は参加必須** → 全参加者の属性が**100%**揃う前提で設計してよい。
- NFCタップ＝参加者のみが計上される → これは全観光客の母集団ではなく**「参加者サンプル」**。人流データ等との併用を将来の補完手段として想定。

---

## 4. 確定済みの設計判断（勝手に変更しないこと）

ユーザーとの合意済み。逆戻りは不可。

1. **地名は画面に出さない**（白浜想定だが非表示）。
2. **「リピーター」という語は使わない**。初開催で過去がなく意味が通らないため。中段は **「じっくり周遊した人（＝3スポット以上を巡った人）」** に置換済み。
3. **ふるさと納税・EC転換はスコープ外**（現段階では先すぎる）。KPI・定義・脚注すべてから削除済み。
4. **配色は明るく普通のダッシュボード**（白背景＋ブルー基調、関係人口の段のみアンバー強調）。**暗い藍テーマは却下済み**。
5. **読み手は2層**：`まち全体ビュー`（地域全体）／`事業者ビュー`（自店のみ）。ロール切替で**見える範囲そのものが変わる**設計。「将来は事業者にも解放」という方針を構造として先に持たせている。
6. **プライバシー設計**：個人名・生年月日は画面に出さない。生年月日は年代算出にのみ使用。**集計・匿名化のみ**を表示し、少人数セグメントは伏せる（k-匿名性）。

---

## 5. 現状の画面構成

### トップバーのイベント選択
- **イベントプルダウン**（`第1回 / 第2回 / 第3回`）：選択するとダッシュボード全体のデータが切り替わる。
- **期間表示**：選択イベントの開催期間を隣に表示（例：`2026.01.10 – 02.09`）。
- **「過去イベントの変遷 →」ボタン**：クリックで変遷ビュー（後述）にフルページ切替。

### 単回ダッシュボード（dashView）
以下のセクションで構成。第2章（ファネル表示）は変遷ビューに移動のため削除済み。

| セクション | 内容 |
|---|---|
| ① 来訪者サマリー | カルテ（4KPI）、流入元、来訪者プロファイル |
| ② 回遊の流れ | スポット別来訪マップ、スポットヒートタイル、導線 |
| ③ 来訪の傾向 | 日次トレンド棒グラフ、曜日×時間帯ヒートマップ |
| ④ スポット別来訪者 | スポット別来訪者表（常時表示） |

### ろか指標ファネル（3段）の位置づけ変更
以前は画面下部に「第2章」として表示していた3段ファネルは**変遷ビュー**に移動。単回ビューには表示しない。

| 段 | ラベル | 定義（画面表示） |
|---|---|---|
| 1 | 参加者 | ろかID登録のうえNFCを押して参加した人（属性100%の母数） |
| 2 | じっくり周遊した人 | 3スポット以上を巡った人。その場で帰らず回遊した |
| 3 | 関係人口の芽 | イベント後も関わる意思を示した人＝**通知ON・フォロー・期間中の再訪**のいずれか |

各段の間に**転換率（%）**を表示。3段目のみアンバーで強調。

### 過去イベントの変遷ビュー（histView）
**まち全体ビュー専用**のフルページ切替ビュー。トップバーの「過去イベントの変遷 →」で遷移し、「← ダッシュボードに戻る」で元の画面に戻る。

構成：
1. **サマリーKPI**（4枚）：開催回数 / 累計参加者 / 直近回の参加者 / 関係人口の芽（累計）
2. **参加者・ろか指標の推移**：イベントごとのグループバー（参加者 / じっくり周遊 / 関係人口の芽）
3. **ろか指標の詳細比較**：テーブル形式でファネル転換率を回別に横並び表示。前回比（%）も表示。
4. **関係人口の芽 — 回別の成長**：水平バーで各回の芽の数を可視化。再訪メモと累計表示付き。

### KPIストリップ（まち全体ビュー）
参加者数 ／ 新規ろかID登録 ／ 延べ来訪（タップ） ／ 平均周遊スポット ／ 景品到達率

### パネル（まち全体ビュー）
1. **期間全体の動向（日次）**＝俯瞰ビュー。イベント期間の日次参加者を棒で並べ、土日・祝を濃色で強調。平日の谷と週末の山が一目で分かる。バーにホバーで日付＋人数。
2. **スポット別の来訪量（ヒートマップ）**。色が濃いほど集客しているスポット。素通りされている地点も分かる。
3. **人気の導線**。スポット間遷移（A▸B）のランキング。
4. **曜日×時間帯ヒートマップ**。
5. **流入元**（出身地の構成バー）。
6. **来訪者プロファイル**（性別×年代）。
7. **スポット別来訪者表**。

### 事業者ビューの差分
同じ枠組みで、自店データに切替：
- ファネル：自店に来訪 → 自店からじっくり周遊 → 自店が起点の関係人口の芽
- KPI：自店の周遊内シェア／時間帯比率／平均滞在（推定）／自店からの送客数／自店経由の新規ろかID登録
- ヒートマップ：**「自店の客がほかに巡ったスポット」**に差し替え（送客・連携先探し）
- 導線：自店の前後に寄られたスポット
- 表：自店の時間帯別来訪

---

## 6. デザイントークン（現状）

**カラー**
- 地：`#eef1f5` ／ カード：`#ffffff`
- 文字：`#1f2a37` ／ 弱文字：`#697586` ／ 罫線：`#e3e8ef`
- プライマリ（青）：`#2b6cb0` ／ 淡青：`#cfe0f1`
- **関係人口アクセント（アンバー）：`#d9842a`**（背景 `#fdf2e2`）
- 転換（緑）：`#2f8f5b`
- ヒートマップ5段：`#e9eef4` → `#cdddee` → `#9cc0e0` → `#5e95c9` → `#2b6cb0`

**タイポグラフィ**
- 和文ゴシック：`Hiragino Kaku Gothic ProN, Yu Gothic, Noto Sans JP, sans-serif`
- 見出しは bold。数値は `font-variant-numeric: tabular-nums`。

**レイアウト**
- 最大幅 1240px。上部に sticky バー（タイトル＋ロール切替＋期間＋店舗選択）。
- カード型2カラムグリッド（モバイル1カラム）。
- アクセシビリティ：`focus-visible` の輪郭、`prefers-reduced-motion` 尊重。

---

## 7. ブラッシュアップで取り組んでほしい論点（優先度順）

1. **周遊動線のサンキー図化**（最優先）。現状はA→Bのランキング止まり。A→B→Cの流れを面で見せたい。スポット間の遷移量を太さで。
2. **日次推移の指標切替**。現状は「参加者数」棒のみ。「景品到達者数」「じっくり周遊した人」へ切り替えられると、関係人口観点でより刺さる。
3. **スポットヒートマップの地理化**。現状は抽象タイル。簡易マップ上にスポットを配置し、円のサイズ／色で来訪量を表せると「どこに流れたか」がより直感的になる。※各スポットの座標データが必要。
4. **「関係人口の芽」の指標精緻化**。通知ON／フォロー／再訪をどう重み付けるか。単一スコアにまとめる是非。
5. **国籍軸（将来）の組み込み余地**。インバウンド分析パネルの設計案。
6. **k-匿名性のUI表現**。少人数セグメントを「伏せている」ことを画面でどう示すか。
7. **事業者ビューの権限の明示**。誰が何を見られるかをUI上で分かるように。
8. **提案書用の"効果の語り口"**。このダッシュボードが自治体の政策判断（誘客の的・動線改善・閑散期対策）にどう効くかを一文で。

---

## 8. やってはいけないこと（リグレッション防止）

- 地名を画面に出す
- 「リピーター」という語を復活させる
- ふるさと納税・EC転換のKPIを足す
- 暗い（藍ベースの）テーマに戻す
- 個人名・生年月日を画面に表示する

いずれもユーザーとの議論で却下済み。

---

## 9. サンプルデータ構造（現行HTML内）

- 単一の self-contained HTML（バニラJS、ビルド不要）。**localStorage等のブラウザストレージは不使用**。
- 構成：`GOV` / `GOV2` / `GOV3`（自治体ビュー・回別）＋ `STORES` / `STORES2` / `STORES3`（店舗別・回別）＋ `EVENTS`配列 ＋ 各 `render*` 関数 ＋ `setMode()` / `setEvent()`。**数値はすべてダミー**。

### EVENTS配列
```javascript
const EVENTS = [
  { id, label, period, evtStart: Date, evtDays: number, gov: GOV, stores: STORES },
  ...
];
```
`activeEventIdx`（`let`変数）でどの回を表示中か管理。`activeEvent()`で現在のイベントオブジェクトを取得。

### GOV / GOV2 / GOV3 の主要キー
- `funnel[]`：`{cap, n, unit, def}`（ヒーロー3段。参加者→じっくり周遊→関係人口の芽）
- `daily[]`：日次参加者数（evtDays分）
- `spotHeat`：`[[スポット名, 人数], ...]`
- `heat[][]`：曜日×時間帯（7行×6列）
- `inflow`：`[[地域名, %], ...]`
- `demo`：`{gender:[[ラベル,%]], age:[[ラベル,%]]}`
- `flow[]`：`{a, b, n, tag?}`（導線）
- `thead[]` / `rows[][]`（ドリルダウン表）
- `timeSlots`：`[[時間帯, 人数], ...]`（まち全体参考値用）

### STORES / STORES2 / STORES3
`STORES[店名]`に同型の上書きデータ ＋ `factor`（日次スケール係数）。`setMode('biz')`時に`Object.assign({}, activeEvent().gov, S)`でマージ。

### イベント切替の流れ
1. `eventSel`プルダウンで`setEvent(idx)`を呼ぶ
2. `activeEventIdx`を更新し、マップの日付範囲リセット
3. `setMode(mode)`を再実行して全パネルを再描画

---

## 10. 引き継ぎ時のお願い

- まず「4」と「8」を読む。確定事項を崩さない範囲で「7」を進める。
- 主役は**関係人口化のファネル**。グラフを増やすより、ファネル→各パネルが「なぜ関係人口に効くか」で一貫しているかを優先。
- 提案先は**自治体観光課の職員**（データ専門家ではない）。専門用語より「平日の谷」「どこに流れたか」のような平易な言葉を保つ。
